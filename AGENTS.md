# 华为杯数学建模竞赛项目

## 项目目标

本项目用于协助完成华为杯数学建模竞赛：理解题目、建立数学模型、编写与验证代码、整理实验结果，并形成可复现的论文材料。

## 协作约定

- 先阅读现有代码、数据、题目说明和实验记录，再进行修改。
- 优先采用 Python 科学计算生态；新增依赖前先检查项目已有环境与依赖文件。
- 所有数据处理、模型假设、参数来源和实验结论都应可追溯，避免无依据地编造结果。
- 修改代码后进行与改动范围匹配的验证，并在回复中说明验证方式和未覆盖的风险。
- 保持改动聚焦，不删除或覆盖用户已有内容，不提交临时文件、缓存和本地环境配置。
- 生成论文或报告时，保持符号、单位、图表编号和结论之间的一致性。

## 默认目录约定

- `research/`：外部资料索引、趋势分析和提示词工作流；先读 `research/README.md`。
- `data/`：原始数据与经过说明的数据文件。
- `src/`：可复用的分析、建模和绘图代码。
- `prompts/`：经过验证的阶段化提示词模板。
- `scripts/`：可重复运行的数据处理、训练、评估和构建脚本。
- `experiments/`：实验配置、运行记录和可复核的比较结果。
- `notebooks/`：探索性分析与过程记录。
- `results/`：模型输出、表格和图片等可复核结果。
- `paper/`：论文、摘要和答辩材料。

## 数学建模专用要求

- 先做题目和数据侦察，再提出候选模型；不得在没有读取题面和数据字典时直接编写最终方案。
- 任何实验数字、引用、题面事实和竞赛规则都必须有来源；无法确认时标记为“待确认”。
- 复杂模型必须与透明 baseline 在一致的数据切分和指标下比较，并报告随机种子、数据泄漏检查、敏感性和局限性。
- 论文中的关键数字必须能追溯到 `results/`、运行日志或生成它们的脚本。
- 比赛期间使用外部资料、联网服务或生成式 AI 前，先核对当届官方竞赛规则和 AI 工具使用规定。

## 资料目录导航与使用规则

### 快速定位顺序

处理任何新题目或新任务时，按以下顺序定位资料，不要一开始扫描整个仓库或全部 PDF：

1. 先读本文件 `AGENTS.md`，确认项目边界、合规要求和输出约定。
2. 读 `research/README.md`，了解资料库结构和使用限制。
3. 读 `research/source-catalog.md`，按来源等级查找官方规则、题面、论文和代码。
4. 读 `research/problem-trends.md`，了解相近年份、题型和常见方法；它是趋势总结，不是题面事实的替代品。
5. 根据当前年份和题号，定位 `references/` 中少量同题论文，再读对应的 `research/notes/` 阅读卡片。
6. 最后才读取相关代码、数据、实验结果和论文文件，形成当前任务所需的最小上下文。

### 各资料目录的职责

| 路径 | 内容 | 使用方式 |
| --- | --- | --- |
| `research/README.md` | 资料库总说明、版权和下载规则 | 每次开始资料型任务时先读 |
| `research/source-catalog.md` | 来源链接、来源等级、用途和风险 | 查找外部资料的唯一入口；先看 A/B 级来源 |
| `research/problem-trends.md` | 2023--2025 赛题矩阵和趋势归纳 | 用于提出候选方向和准备学习计划，不用于替代当届题面 |
| `research/prompt-playbook.md` | 侦察、建模、实现、验证和论文写作提示词 | 编写或执行复杂任务提示词前先参考 |
| `research/notes/` | 针对具体论文、题目或算法的阅读卡片 | 记录页码、来源、方法、复现性和疑点；禁止把摘要当作完整复现 |
| `research/library.db` | 论文和 Markdown 的本地全文检索数据库 | 先用检索脚本定位候选资料，再按需读取原文件；数据库由脚本生成，不手工编辑 |
| `references/` | 2023--2025 优秀论文 PDF 本地缓存 | 只按题号、年份和研究问题按需读取；不要批量扫描 |
| `prompts/` | 已验证的阶段化提示词模板 | 优先复用模板，再根据题目补充上下文和约束 |
| `data/raw/` | 原始题面数据和原始外部数据 | 只读保存，不直接覆盖或手工修改 |
| `data/interim/` | 清洗、转换和特征处理中间产物 | 必须由 `scripts/` 或可复现 Notebook 生成 |
| `data/processed/` | 最终模型输入数据 | 记录生成脚本、参数、版本和数据切分 |
| `src/` | 可复用的数据处理、建模、评估和绘图代码 | 优先修改这里，不把核心逻辑长期留在 Notebook |
| `scripts/` | 可重复执行的入口脚本 | 记录命令、参数、输入和输出 |
| `experiments/` | 实验配置、运行日志和模型比较 | 保存随机种子、数据切分、指标和环境信息 |
| `results/` | 可用于论文的表格、图片和汇总结果 | 每个关键数字必须能追溯到脚本和实验记录 |
| `paper/` | 论文、参考文献和答辩材料 | 只引用已核验的结果；不要在此目录直接生成未经验证的结论 |

### 资料数据库的使用方式

- 数据库构建脚本为 `scripts/build_library.py`，查询脚本为 `scripts/search_library.py`。
- 资料新增、删除或 PDF 更新后，运行 `python scripts/build_library.py --force` 重建 `research/library.db`；不要直接修改 SQLite 文件。
- 检索优先使用关键词、年份和题号过滤，例如：
  `python scripts/search_library.py "迁移学习" --year 2025 --problem E`。
- 查询相近内容时，先使用当前题目的领域词，再加入任务词和方法词，例如“轴承 故障诊断 迁移学习”“风电 疲劳 优化”。
- `--kind paper_pdf` 只查论文，`--kind project_markdown` 只查资料说明和阅读笔记；`--json` 便于后续脚本处理。
- 数据库结果只是候选片段，不能替代原始文件。使用结果前必须打开对应 `path`，核对页码、上下文、原始来源和适用条件。
- 如果中文关键词没有全文命中，改用同义词、拆分词或题号/年份过滤；不要据此断言资料不存在。
- 读取论文时默认选取 2--5 个最相关结果，优先同年同题，其次同任务类型跨年资料，最后才扩展到其他题型。

### 来源与证据规则

- 竞赛规则、赛程、格式、AI 使用限制和当届题面：只以官方平台或官方文件为准。
- 题型趋势、算法启发和写作结构：可参考 B 级公开仓库和论文，但必须注明来源，不能当作官方结论。
- 博客、视频、论坛和个人经验：只用于发现关键词和候选方法，不能单独支撑事实、引用、获奖信息或评委偏好。
- 优秀论文中的数字、图表和结论属于原作者结果；引用时记录论文文件、页码和上下文，不能直接改写成当前项目结果。
- 代码仓库中的结果必须先检查数据泄漏、数据切分、随机种子、依赖版本和运行路径，再决定是否复现。
- 任何无法从当前文件或可靠来源确认的内容标记为“待确认”，禁止猜测、补写或伪造引用。

### 论文资料的按需读取规则

- 先选择与当前题目同年同题的论文，再扩展到同任务类型的跨年论文，最后才看其他题型。
- 单次任务默认读取 2--5 篇最相关论文；只有在明确需要综述时才扩大范围。
- 阅读论文时优先提取：问题拆解、假设、符号、数据处理、baseline、核心模型、验证设计、敏感性分析和局限性。
- 不要让模型仅凭论文标题或摘要推断算法细节；涉及公式、参数和实验设置时必须读取正文对应章节。
- 每完成一次有价值的阅读，在 `research/notes/` 写入简短卡片，至少包含来源、题目、页码、方法、可复现性和可迁移经验。

### 新资料的归档规则

- 新增资料前先检查 `research/source-catalog.md`，避免重复下载或引入低质量镜像。
- 原始 PDF、压缩包和大数据集放在 `references/` 或 `data/raw/`，不放在 `research/`、`src/` 或 `paper/`。
- 外部资料默认只保存链接和摘要；只有对当前任务有明确帮助且来源/版权边界可接受时才下载。
- 下载资料时记录来源 URL、下载日期、文件名、用途；需要长期复现时再记录 SHA-256 哈希。
- 不自动安装来历不明的插件、脚本、依赖或浏览器扩展；先说明用途、来源、权限和替代方案。

## 标准任务输出要求

完成任何实质性建模任务后，回复中简要说明：

- 使用了哪些资料文件和外部来源；
- 做了哪些代码、数据或论文修改；
- 采用了哪些假设、数据切分和评价指标；
- 执行了什么验证命令及其结果；
- 哪些结论是已验证事实，哪些仍是推断或待确认事项。

如果任务涉及写论文，先生成或更新结果表、图和证据清单，再写正文；如果任务涉及代码，先说明输入输出契约和验证计划，再修改文件。

如项目后续形成不同目录结构，以实际结构为准，并同步更新本文件。
