# Codex 工作上下文恢复文件

本文件用于在新的 Codex 上下文中恢复 `D:\\MCMM\\23rd-MCMM` 项目的工作状态。读取本文件后，必须先读取同目录下的 `AGENTS.md`，以获得项目约束、数据边界、输出目录规则和论文写作要求。

## 1. 项目定位

这是华为杯中国研究生数学建模竞赛 2026 年第二十三届 E 题项目，题目为“复杂场景下多模态情感预测的数学建模与算法设计”。当前工作集中在 `E-q/`，包括：

- 问题一：文本、语音、视觉特征提取与时序对齐；
- 问题二：局部模态缺失下的鲁棒情感预测；
- 问题三：可解释性多模态情感预测。

团队负责最终建模假设、核心论证和竞赛论文定稿。Codex 负责代码、数据处理、可复现输出、图表、审计和论文辅助材料，不能编造数据、结果或参考文献。

## 2. 必须先读取的文件

按以下顺序读取：

1. `AGENTS.md`
2. 根目录 `README.md`
3. 与当前任务对应的 `versions/` 文件
4. 与问题对应的 `E-q/modeling/q_*.md`
5. 当前输出目录的 `README.md`、配置、审计和数据文件

资料型任务还要按 `AGENTS.md` 的规定读取 `research/README.md`、`research/source-catalog.md`，论文撰写任务还要读取 `references/README.md`。不要默认把外部论文指标当作当前项目结果。

## 3. 重要目录

| 路径 | 用途 |
|---|---|
| `E-q/` | E 题工作目录、题面副本、数据集、代码和结果 |
| `E-q/dataset/` | 赛题附件和特征文件，只读使用 |
| `src/` | 可复用实现 |
| `scripts/` | 可重复运行脚本 |
| `cli/` | 命令行入口 |
| `test/` | 测试 |
| `modeling/` | 建模说明 |
| `draft/` | 论文草稿，需区分草稿和已验证结论 |
| `versions/` | 版本要求和分析 |
| `CMU-MOSEI/` | 外部专题资料，仅用于方法参考 |
| `research/` | 资料索引、阅读卡片和全文检索库 |

## 4. 当前问题三结果

最近一次完整问题三结果目录为：

`E-q/q_3_output/20260925_225153_740084700/`

该目录包含一次完整的问题三论文支撑材料包，已生成：

- valid/test 性能、分类报告、混淆矩阵和回归诊断；
- 模态贡献、后验模态移除、门控与反事实贡献一致性；
- valid/test/附件四的局部重要性和证据映射；
- 附件四 20 条无标签样本的预测结果；
- 20 张证据卡、60 张模态关键帧及 JSON 元数据；
- 训练曲线、模型结构图、技术路线图、数据边界图；
- 视频解码帧数审计、完整性审计、输入清单和哈希校验；
- 目录内详细说明文件 `E-q/q_3_output/20260925_225153_740084700/README.md`。

该结果目录的 README 已逐项解释所有顶层 CSV、JSON、PNG、LaTeX、NPZ、模型文件、关键帧和证据卡。继续处理问题三结果时，应先读取该 README，不要重复猜测字段含义。

## 5. 当前问题三关键结果

来自 `model_metrics.csv`：

| 划分 | Accuracy | Macro-F1 | MAE | Pearson |
|---|---:|---:|---:|---:|
| valid | 0.607143 | 0.547565 | 0.652097 | 0.580993 |
| test | 0.672627 | 0.595023 | 0.679413 | 0.625911 |

其他已确认事实：

- train/valid/test 样本数为 3395/728/727；附件四为 20 个无标签样本；
- 特征版本为 `aligned`，对齐长度 50；text/audio/vision 维度分别为 768/74/35；
- 类别映射为 `0=Negative`、`1=Neutral`、`2=Positive`；
- 解释采用训练集均值 `train_mean` 反事实遮挡，窗口和步长均为 7，Top-k 为 3；
- `impact_score` 表示模型敏感性，不表示因果效应或真实情绪成因；
- `modality_ablation_metrics.csv` 是固定模型的后验模态移除诊断，不是重新训练的单模态基线；
- 附件四只能报告预测和解释，不能报告真实性能；
- 附件四有 16 个视频的容器声明帧数与实际解码帧数不一致，证据映射以实际解码帧审计为准；
- 当前结果只有随机种子 `20260924`，不能声称多种子稳定性、置信区间或显著性；
- 门控与反事实贡献相关性较弱，门控不能直接替代解释证据；
- 文本证据的时间是对齐相对窗口，不是逐词精确时间戳。

## 6. 当前论文草稿

问题三 v3 的论文材料位于：

- `draft/q_3/v3/q_3.md`
- `draft/q_3/v3/q_3.tex`
- `draft/q_3/v3/q_3.pdf`
- `draft/q_3/v3/assets/`

v3 已将 `20260925_225153_740084700` 的实际表格和图表纳入完整问题三论文，并已通过 XeLaTeX 编译为 20 页 A4 PDF。编译命令为：

```powershell
cd D:\\MCMM\\23rd-MCMM\\draft\\q_3\\v3
xelatex -interaction=nonstopmode -halt-on-error q_3.tex
xelatex -interaction=nonstopmode -halt-on-error q_3.tex
```

编译无致命错误、图片缺失或页面裁切；日志中存在 `Underfull \\hbox`、MiKTeX 版本日期和更新提示等非致命警告。

## 7. 最近一次已完成的变更

最近完成的用户任务是：完善结果目录 `E-q/q_3_output/20260925_225153_740084700/README.md`，详细说明该目录所有文件、字段、图表、模型材料、解释含义和论文用法。

当前恢复文件本身是新增文件。不要为了恢复上下文而回滚或覆盖用户已有修改。`output_checksums.json` 中 README 的旧哈希没有更新，这是因为用户明确要求只修改 README，其他文件不得修改。

## 8. 输出目录规则

每次问题一、二、三运行都必须在对应目录下创建新的时间戳子目录：

```text
E-q/q_1_output/<YYYYMMDD_HHMMSS_microseconds>/
E-q/q_2_output/<YYYYMMDD_HHMMSS_microseconds>/
E-q/q_3_output/<YYYYMMDD_HHMMSS_microseconds>/
```

不得覆盖历史结果，不得把不同问题的结果混合。CSV 使用 UTF-8 BOM；对可能被 Excel 解释为公式的文本字段要做 Excel 安全处理。每个运行目录应保存配置、随机种子、输入清单、日志、结果、图表和异常记录。

## 9. 数据边界

- 只使用赛题提供的数据进行训练、调参、阈值选择和性能统计；
- 附件二 train/valid/test 边界必须保留；标准化和参数选择只能使用训练集或验证集；
- 附件三、附件四无标签专项集只能用于最终推理，不能用于训练、调参、早停或真实性能统计；
- `aligned_50.pkl` 与 `unaligned_50.pkl` 不可混用；`text` 与 `text_bert` 只能二选一；
- 题面、附件和原始数据只读，不直接覆盖或手工修改；
- 不扫描、修改或分析其他赛题材料，除非用户明确要求。

## 10. 新上下文的推荐恢复动作

如果用户没有提出新的明确任务，先执行以下只读检查：

```powershell
Get-Content -Raw AGENTS.md
Get-Content -Raw CODEX_CONTEXT.md
Get-Content -Raw E-q/q_3_output/20260925_225153_740084700/README.md
git status --short
```

如果用户继续要求问题三论文、图表或结果分析：

1. 先确认是否指定了新的 `versions/v*.md`；
2. 读取对应建模文件和当前结果 README；
3. 区分实际运行结果、外部资料结论和推断；
4. 不生成未由 CSV/JSON/日志支撑的数字；
5. 修改前说明输入、输出和验证计划；
6. 完成后运行相称的测试或编译，并报告未覆盖风险。

如果用户要求修改问题三代码，应优先检查 `src/`、`scripts/`、`cli/`、`test/` 和 `E-q/modeling/q_3.md`，不要只修改论文或结果文件来掩盖代码问题。

## 11. 对恢复后的 Codex 的硬性提醒

- 先读 `AGENTS.md`，其中的约束优先于本文件的摘要；
- 不要编造数据、参考文献、实验结果或标签；
- 不要把附件四预测写成有真实性能的测试结果；
- 不要把后验模态移除写成重新训练的单模态基线；
- 不要把 `impact_score` 写成因果解释；
- 不要使用危险的 Git 回滚命令覆盖用户修改；
- 若任务只要求分析，不要擅自修改代码或数据；若任务要求实现，必须完成验证并说明结果；
- 继续工作时将本文件视为上下文摘要，将实际文件和日志视为最终事实来源。
